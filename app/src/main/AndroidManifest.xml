<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Audio permission for Speech-to-Text -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Storage permissions for image upload -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" 
        android:maxSdkVersion="28" />

    <!-- Notification permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Boot receiver for rescheduling reminders -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Foreground service for reminders -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icon_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorldCup2026SmartGuide"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDSvvk57l5YSCSfa7bHPdR_IpOg3ikemE0" />

        <!-- Firebase Messaging Service -->
        <service
            android:name=".notifications.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- WorkManager initialization -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <!-- Boot receiver for rescheduling reminders -->
        <receiver
            android:name=".notifications.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- Splash Activity (Launcher) -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SplashScreen"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Profile Activity -->
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Favorites Activity -->
        <activity
            android:name=".ui.favorites.FavoritesActivity"
            android:exported="false"
            android:theme="@style/Theme.WorldCup2026SmartGuide" />

        <!-- Comments Activity -->
        <activity
            android:name=".ui.comments.CommentsActivity"
            android:exported="false"
            android:theme="@style/Theme.WorldCup2026SmartGuide" />

        <!-- Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.WorldCup2026SmartGuide" />

        <!-- Main Activity -->
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.WorldCup2026SmartGuide.MainActivity">

            <!-- Deep link support -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="wcguide" />
            </intent-filter>
        </activity>

        <!-- Login Activity -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Register Activity -->
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Contact Us Activity -->
        <activity
            android:name=".ui.contact.ContactUsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- About Us Activity -->
        <activity
            android:name=".ui.about.AboutUsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Privacy Policy Activity -->
        <activity
            android:name=".ui.privacy.PrivacyPolicyActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Terms of Service Activity -->
        <activity
            android:name=".ui.terms.TermsOfServiceActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Event Details Activity -->
        <activity
            android:name=".ui.details.event.EventDetailsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Place Details Activity -->
        <activity
            android:name=".ui.details.place.PlaceDetailsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- All Hotels Activity -->
        <activity
            android:name=".ui.hotels.AllHotelsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar"
            android:label="جميع الفنادق" />

        <!-- All Restaurants Activity -->
        <activity
            android:name=".ui.restaurants.AllRestaurantsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar"
            android:label="جميع المطاعم" />


        <!-- Banner Details Activity -->
        <activity
            android:name=".ui.banner.BannerDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Logos Activity -->
        <activity
            android:name=".ui.logos.LogosActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:label="@string/logos_title"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Admin Activity (for content management) -->
        <activity
            android:name=".ui.admin.AdminActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:label="@string/admin_panel_title"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Manage Banner Activity -->
        <activity
            android:name=".ui.admin.ManageBannerActivity"
            android:exported="false"
            android:parentActivityName=".ui.admin.AdminActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Manage Landmarks Activity -->
        <activity
            android:name=".ui.admin.ManageLandmarksActivity"
            android:exported="false"
            android:parentActivityName=".ui.admin.AdminActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Add/Edit Landmark Activity -->
        <activity
            android:name=".ui.admin.AddEditLandmarkActivity"
            android:exported="false"
            android:parentActivityName=".ui.admin.ManageLandmarksActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Landmark Details Activity -->
        <activity
            android:name=".ui.admin.LandmarkDetailsActivity"
            android:exported="false"
            android:parentActivityName=".ui.admin.ManageLandmarksActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Add Review Activity -->
        <activity
            android:name=".ui.admin.AddReviewActivity"
            android:exported="false"
            android:parentActivityName=".ui.admin.LandmarkDetailsActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Add Landmark Rating Activity -->
        <activity
            android:name=".ui.admin.AddLandmarkRatingActivity"
            android:exported="false"
            android:parentActivityName=".ui.admin.ManageReviewsActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Hotel Management Activity -->
        <activity
            android:name=".ui.admin.HotelManagementActivity"
            android:exported="false"
            android:parentActivityName=".ui.admin.AdminActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Add/Edit Hotel Activity -->
        <activity
            android:name=".ui.admin.AddEditHotelActivity"
            android:exported="false"
            android:parentActivityName=".ui.admin.HotelManagementActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Restaurant Management Activity -->
        <activity
            android:name=".ui.admin.RestaurantManagementActivity"
            android:exported="false"
            android:parentActivityName=".ui.admin.AdminActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Add/Edit Restaurant Activity -->
        <activity
            android:name=".ui.admin.AddEditRestaurantActivity"
            android:exported="false"
            android:parentActivityName=".ui.admin.RestaurantManagementActivity"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Manage Events Activity -->
        <activity
            android:name=".ui.admin.ManageEventsActivity"
            android:exported="false"
            android:parentActivityName=".ui.admin.AdminActivity"
            android:label="إدارة الفعاليات"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Add/Edit Event Activity -->
        <activity
            android:name=".ui.admin.AddEditEventActivity"
            android:exported="false"
            android:parentActivityName=".ui.admin.ManageEventsActivity"
            android:label="إضافة/تعديل فعالية"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Event Details Activity (Public) -->
        <activity
            android:name=".ui.events.EventDetailsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:label="تفاصيل الفعالية"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Manage Reviews Activity -->
        <activity
            android:name=".ui.admin.ManageReviewsActivity"
            android:exported="false"
            android:parentActivityName=".ui.admin.AdminActivity"
            android:label="إدارة التقييمات"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- All Landmarks Activity (public view) -->
        <!-- Fixed: specify full qualified name since the class is in package com.example.wcguide.activities -->
        <activity
            android:name="com.example.wcguide.activities.AllLandmarksActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:label="جميع المعالم السياحية"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Hotel Details Activity -->
        <activity
            android:name=".ui.details.hotel.HotelDetailsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:label="تفاصيل الفندق"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Restaurant Details Activity -->
        <activity
            android:name=".ui.details.restaurant.RestaurantDetailsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:label="تفاصيل المطعم"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Firebase Debug Activity -->
        <activity
            android:name=".debug.FirebaseDebugActivity"
            android:exported="false"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- News Activity -->
        <activity
            android:name=".ui.news.NewsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:label="@string/news_title"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- News Detail Activity -->
        <activity
            android:name=".ui.news.NewsDetailActivity"
            android:exported="false"
            android:parentActivityName=".ui.news.NewsActivity"
            android:label="@string/news_detail_title"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Events Activity -->
        <activity
            android:name=".ui.events.EventsActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:label="@string/nav_events"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- About App Activity -->
        <activity
            android:name=".ui.about.AboutAppActivity"
            android:exported="false"
            android:parentActivityName=".ui.main.MainActivity"
            android:label="@string/about_app_title"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Admin News Activity -->
        <activity
            android:name=".ui.admin.AdminNewsActivity"
            android:exported="false"
            android:parentActivityName=".ui.admin.AdminActivity"
            android:label="@string/admin_news_title"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

        <!-- Add/Edit News Activity -->
        <activity
            android:name=".ui.admin.AddEditNewsActivity"
            android:exported="false"
            android:parentActivityName=".ui.admin.AdminNewsActivity"
            android:label="@string/add_edit_news_title"
            android:theme="@style/Theme.WorldCup2026SmartGuide.NoActionBar" />

    </application>

</manifest>